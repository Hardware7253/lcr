// The UI module runs a measurement when the user makes an input
// After a measurement has completed the information is displayed back to the user

#pragma once

#include <stdint.h>

void init_ui(void);
void run_ui(void);